@model IEnumerable<Lab3.Models.Tbl_Payment_Method>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pa_Method)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pa_Method)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Pa_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Pa_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Pa_ID })
        </td>
    </tr>
}

</table>
